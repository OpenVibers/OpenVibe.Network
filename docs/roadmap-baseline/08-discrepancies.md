# Production vs repository

> Generated by `scripts/roadmap-baseline/generate.js`. Do not edit by hand; edit `data/*.json` or the extractor and regenerate.

Deliverable 7. Snapshot of openvibe-oregon taken 2026-09-23 20:32 UTC compared with the local checkouts. `unknown` rows are things this environment cannot see.

| Severity | Area | Subject | Detail |
|---|---|---|---|
| medium | deploy | ai | deployed 96dc8a3 is 1 commit(s) behind origin/main 3a480ac |
| medium | deploy | billing | deployed c646279 is 1 commit(s) behind origin/main aa445d0 |
| medium | deploy | chat | deployed 7897688 is 2 commit(s) behind origin/main ed90461 |
| medium | deploy | codes | deployed d60f279 is 1 commit(s) behind origin/main 51d2386 |
| medium | deploy | community | deployed 57d1dd4 is 1 commit(s) behind origin/main 63b90a8 |
| medium | deploy | coupons | deployed 54df411 is 3 commit(s) behind origin/main 1c57c47 |
| medium | deploy | deals | deployed e72aeec is 2 commit(s) behind origin/main 91ca223 |
| medium | deploy | events | deployed 3366132 is 1 commit(s) behind origin/main efc09a7 |
| medium | deploy | games | deployed 474df50 is 1 commit(s) behind origin/main 7863fc6 |
| medium | deploy | host | deployed 30f08d6 is 15 commit(s) behind origin/main 03dfcb3 |
| medium | deploy | live | deployed c893c6c is 12 commit(s) behind origin/main ca7cc87 |
| medium | deploy | media | deployed 9de4b25 is 4 commit(s) behind origin/main ad2217b |
| medium | deploy | network | deployed 9bbd90f is 4 commit(s) behind origin/main 09bf748 |
| medium | deploy | news | deployed 5a17e7f is 1 commit(s) behind origin/main 5b393d2 |
| medium | deploy | openre | deployed e0dc1b4 is 2 commit(s) behind origin/main c091a09 |
| medium | deploy | reviews | deployed 1d462a2 is 1 commit(s) behind origin/main a7b50a6 |
| medium | deploy | sites | deployed d3b71af is 2 commit(s) behind origin/main d6e4cbd |
| medium | deploy | tips | deployed 15bfc12 is 6 commit(s) behind origin/main f211c03 |
| medium | deploy | trade | deployed 9a55644 is 2 commit(s) behind origin/main ddbc60c |
| medium | deploy | vip | deployed 9668a49 is 1 commit(s) behind origin/main fe7eaad |
| medium | deploy | wiki | deployed 011a68f is 1 commit(s) behind origin/main b4e70ac |
| low | database | /opt/openvibe.community/data/community.db | 115 KB, 9 tables, not a database the service opens (services.json): stale file or undeclared copy |
| low | database | /opt/openvibe.games/data/legacy-import/world-before-20260923T184930Z.db | 193 KB, 13 tables, not a database the service opens (services.json): stale file or undeclared copy |
| low | database | /opt/openvibe.live/data/backups/analytics-pre-adr021-2026-09-23.db | 1.76 GB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.live/data/backups/live-pre-643a2e6.db | 264.7 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.live/data/hobo.db | 0 B, zero tables (dead file) |
| low | database | /opt/openvibe.live/data/openvibe.db | 0 B, zero tables (dead file) |
| low | database | /opt/openvibe.tips/data/tips.db | 4 KB, 15 tables, not a database the service opens (services.json): stale file or undeclared copy |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/audio.analytics.db | 60.3 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/docs.analytics.db | 1.7 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/food.analytics.db | 459 KB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/img.analytics.db | 5.7 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/maps.analytics.db | 590 KB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/text.analytics.db | 9.5 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /opt/openvibe.tools/backups/analytics-2026-09-23/yt.analytics.db | 975 KB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /var/lib/openvibe-billing/live-snapshot-202609230121.db | 279.2 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /var/lib/openvibe-chat/live-snap-a.db | 279.2 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /var/lib/openvibe-chat/live-snap-b.db | 279.2 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /var/lib/openvibe-chat/live-snapshot-0159.db | 279.2 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /var/lib/openvibe-chat/rehearsal.db | 360 KB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | database | /var/lib/openvibe-community/community.pre-live-comments-20260923T191345Z.db | 2.0 MB copy (backup, import snapshot or rehearsal) kept beside the service data |
| low | env | OpenRe.Stream:OPENRE_SECRETS_KEY_PREVIOUS | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:CCBILL_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:NOWPAYMENTS_API_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:NOWPAYMENTS_IPN_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:PAYPAL_CLIENT_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:POWERCHAT_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:STRIPE_SECRET_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Billing:STRIPE_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Chat:CHAT_EVENTS_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Codes:OV_CLIENT_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Community:DISCORD_RELAY_WEBHOOK_VARS | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Community:VIEW_HASH_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Coupons:COUPONS_LOOKUP_TOKEN_PER_MIN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Examples:OV_CHAT_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Examples:OV_CLIENT_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Examples:OV_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Examples:OV_WEBHOOK_SECRET_PREVIOUS | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:GH_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:GITHUB_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:OPS_ALERT_WEBHOOK_URL | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:OV_INTERNAL_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Media:MEDIA_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Media:VIEW_HASH_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:DEV_CREDENTIAL_OVERLAP_S | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:OV_GAMES_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:OV_INTERNAL_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:OV_LIVE_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:SETUP_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Search:CLOUDFLARE_PURGE_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Sources:SOURCES_ALLOW_PRIVATE_HOSTS | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:COOKIE_DOMAIN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:NPS_API_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:OPEN_CHARGE_MAP_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:OV_INTERNAL_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:RIDB_API_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Trade:TRADE_EVENTS_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | schema | OpenVibe.Live:active_effects | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:ai_conversations | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:ai_memory | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:analytics_snapshots | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:arena_achievements | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_battles | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_events | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_talk | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_talk_hype | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_talk_session_hype | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_talk_session_topics | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_talk_sessions | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_talk_topics | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_tier_paid | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topic_hype | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topic_members | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topic_moments | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topic_progress | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topic_sides | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topic_threads | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_topics | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_viewer_clout | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:arena_votes | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:bank_items | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:battle_stats | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:canvas_actions | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:canvas_bans | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:canvas_region_locks | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:canvas_settings | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:canvas_snapshots | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:canvas_tiles | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:canvas_user_overrides | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (Collaborative canvas state (roadmap 14.4); Live code removed in 8fa04cc, data still in live.db.) |
| low | schema | OpenVibe.Live:changelog_entries | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:chat_log | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:chatter_profiles | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:chatter_subjects | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:chatter_xp_log | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (Left behind by the Arena v1/v2 -> v3 rewrite (4ea1a7c); no code reads them.) |
| low | schema | OpenVibe.Live:custom_rewards | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:daily_claims | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:divorce_history | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:dungeon_stats | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:event_log | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:farm_plots | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:file_checksums | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:fish_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:gusher_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:inventory | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:jo_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:love_affinity | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:love_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:love_proposals | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:mine_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:mod_persuasion | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:music_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:promo_claims | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop (RobotStreamer promo cashout ledger; campaign removed in a4c8aea and must not be rebuilt. May hold payout records: archive before dropping.) |
| low | schema | OpenVibe.Live:roast_dossier | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:roast_log | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:unlocked_recipes | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:user_voice_selection | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:user_voices | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| low | schema | OpenVibe.Live:woodcut_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) (RobotStreamer companion game DB (unmodified since Mar 2026); read only by scripts/migrate-rs-companion.js.) |
| info | env | OpenVibe.AI:AI_API_KEY | set in ai.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.AI:OV_OAUTH_CLIENT_ID | set in ai.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.AI:OV_OAUTH_CLIENT_SECRET | set in ai.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Events:OV_OAUTH_CLIENT_ID | set in events.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Events:OV_OAUTH_CLIENT_SECRET | set in events.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Games:NODE_ENV | set in games.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Games:PORT | set in games.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:MIN_CASHOUT | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:OV_LIVE_URL | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:RS_COMPANION_DB_PATH | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:RS_PASSTHROUGH_ROBOTS | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:UNSAFE_WORDS_PATH | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:PASTES_MOVED_TO | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:PORT | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:RTP_PORT_MAX | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:RTP_PORT_MIN | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Network:NOTIFICATION_EMAIL_CRITICAL_ONLY | set in network.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Network:NOTIFICATION_MAX_AGE_DAYS | set in network.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Network:NOTIFICATIONS_ENABLED | set in network.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | schema | OpenRe.Stream:event_outbox | in production (openre.db) but no CREATE TABLE in the repo; classified keep -> OpenRe.Stream (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Blog:blog_index_revisions | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_attachments | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_citation_purges | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_citations | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_discussion_refs | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_drafts | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_reviews | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_revision_purges | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_post_revisions | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_redirects | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_schedule_jobs | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_term_links | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:blog_terms | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Blog:event_outbox | in production (blog.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Blog (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Chat:chat_event_inbox | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Chat:deploy_releases | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Codes:event_outbox | in production (codes.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Codes (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Codes:trust_adr013 | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Coupons:coupons_index_revisions | in production (coupons.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Coupons (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Coupons:event_outbox | in production (coupons.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Coupons (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Deals:deal_index_revisions | in production (deals.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Deals (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Deals:deal_offer_discussion_refs | in production (deals.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Deals (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Deals:event_outbox | in production (deals.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Deals (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Deals:idempotency_receipts | in production (deals.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Deals (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Games:event_outbox | in production (world.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Games (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Host:event_outbox | in production (host.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Host (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Host:host_takedowns | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:analytics_daily | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Live:analytics_day_salts | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Live:analytics_events | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Live:analytics_hourly | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Live:analytics_rate_tracking | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Live:analytics_visitor_days | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Live:billing_actions | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:channel_sounds_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:chat_messages_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:emotes_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:event_outbox | in production (live.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Live (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Live:robotstreamer_integrations_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:stream_controls_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Media:event_outbox | in production (media.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Media (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Media:media_upload_parts | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Media:media_uploads | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Network:admin_rate_limits | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Network:analytics_daily | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Network:analytics_day_salts | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Network:analytics_events | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Network:analytics_hourly | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Network:analytics_rate_tracking | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Network:analytics_visitor_days | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Network:network_event_inbox | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Network:network_event_outbox | in production (network.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Network (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Network:user_module_retirements | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Network:user_module_revisions | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.News:event_outbox | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.News:idempotency_receipts | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.News:news_index_revisions | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_citation_purges | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_citations | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_discussion_refs | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_drafts | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_reviews | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_revision_purges | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.News:news_story_revisions | in production (news.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.News (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_discussion_refs | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_entity_redirects | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_event_inbox | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Reviews:review_event_outbox | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Reviews:review_index_revisions | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_summary_drafts | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_summary_reviews | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_summary_revision_purges | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Reviews:review_summary_revisions | in production (reviews.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Reviews (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Search:fts_public_config | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_public_content | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_public_data | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_public_docsize | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_public_idx | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_restricted_config | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_restricted_content | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_restricted_data | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_restricted_docsize | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Search:fts_restricted_idx | in production (search.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Search (FTS5 shadow tables of the per-audience full-text indexes.) |
| info | schema | OpenVibe.Tips:event_outbox | in production (tips.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tips (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Tips:idempotency_receipts | in production (tips.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tips (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Tips:tip_moderation_log | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Tips:tip_moderator_invites | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Tips:tip_moderators | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Tools:analytics_daily | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Tools:analytics_day_salts | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Tools:analytics_events | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Tools:analytics_hourly | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Tools:analytics_rate_tracking | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Tools:analytics_visitor_days | in production (analytics.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Per-site analytics from openvibe-shared/analytics.js; ADR-021 (2026-09-23): no IP, user id or city stored, raw rows pruned (hazard H11).) |
| info | schema | OpenVibe.Tools:event_outbox | in production (jobs.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Tools (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Trade:event_outbox | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Trade:idempotency_receipts | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Trade:trade_context_drafts | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Trade:trade_context_reviews | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Trade:trade_context_revision_purges | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Trade:trade_context_revisions | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Trade:trade_index_revisions | in production (trade.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Trade (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.VIP:event_outbox | in production (vip.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.VIP (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.VIP:idempotency_receipts | in production (vip.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.VIP (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Wiki:wiki_citation_purges | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_citations | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_discussion_refs | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_event_outbox | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Created at runtime by the openvibe-sdk / openvibe-events outbox and inbox helpers.) |
| info | schema | OpenVibe.Wiki:wiki_index_revisions | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_page_attachments | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_page_drafts | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_page_redirects | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_page_reviews | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_page_revision_purges | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_page_revisions | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| info | schema | OpenVibe.Wiki:wiki_schedule_jobs | in production (wiki.db) but no CREATE TABLE in the repo; classified keep -> OpenVibe.Wiki (Prefixed stores created at runtime by openvibe-publishing (revisions, citations, schedules, redirects, index hooks).) |
| unknown | host | powerchat.gg, raspi/hobo.tools (LAN) | unreachable from this environment; state not verified |
| unknown | provider | B2/R2/Cloudflare/PayPal consoles | console configuration (billing, lifecycle rules, zone settings, app config) is not visible from this environment |
