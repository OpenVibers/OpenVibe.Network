# Production vs repository

> Generated by `scripts/roadmap-baseline/generate.js`. Do not edit by hand; edit `data/*.json` or the extractor and regenerate.

Deliverable 7. Snapshot of openvibe-oregon taken 2026-09-22 23:36 UTC compared with the local checkouts. `unknown` rows are things this environment cannot see.

| Severity | Area | Subject | Detail |
|---|---|---|---|
| low | database | /opt/openvibe.live/data/backups/live-pre-643a2e6.db | 264.7 MB backup inside the service data dir |
| low | database | /opt/openvibe.live/data/hobo.db | 0 B, zero tables (dead file) |
| low | database | /opt/openvibe.live/data/openvibe.db | 0 B, zero tables (dead file) |
| low | deploy | games | deployed 43c11f0 has the same file tree as origin/main 5710287 but a different SHA (pre-rewrite history); reset the checkout so later pulls fast-forward |
| low | env | OpenVibe.Community:COOKIE_SECURE | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Community:VIEW_HASH_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:GH_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:GITHUB_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:OPS_ALERT_WEBHOOK_URL | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Live:OV_INTERNAL_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Media:MEDIA_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Media:VIEW_HASH_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:OV_GAMES_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:OV_INTERNAL_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:OV_LIVE_WEBHOOK_SECRET | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Network:SETUP_TOKEN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:COOKIE_DOMAIN | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:COOKIE_SECURE | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:NPS_API_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:OPEN_CHARGE_MAP_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:OV_INTERNAL_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | env | OpenVibe.Tools:RIDB_API_KEY | secret-named variable read by code but not set in the service env file: verify it is optional or has no hardcoded fallback |
| low | schema | OpenVibe.Live:active_effects | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:ai_conversations | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:ai_memory | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:analytics_snapshots | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:arena_achievements | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_battles | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_events | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_talk | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_talk_hype | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_talk_session_hype | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_talk_session_topics | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_talk_sessions | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_talk_topics | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_tier_paid | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topic_hype | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topic_members | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topic_moments | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topic_progress | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topic_sides | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topic_threads | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_topics | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_viewer_clout | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:arena_votes | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:bank_items | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:battle_stats | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:canvas_actions | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:canvas_bans | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:canvas_region_locks | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:canvas_settings | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:canvas_snapshots | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:canvas_tiles | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:canvas_user_overrides | in production (live.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games |
| low | schema | OpenVibe.Live:changelog_entries | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:chat_log | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:chatter_profiles | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:chatter_subjects | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:chatter_xp_log | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:custom_rewards | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:daily_claims | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:divorce_history | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:dungeon_stats | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:event_log | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:farm_plots | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:file_checksums | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:fish_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:gusher_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:inventory | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:jo_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:love_affinity | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:love_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:love_proposals | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:mine_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:mod_persuasion | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:music_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:promo_claims | in production (live.db) but no CREATE TABLE in the repo; classified investigate -> archive then drop |
| low | schema | OpenVibe.Live:roast_dossier | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:roast_log | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:unlocked_recipes | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:user_voice_selection | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:user_voices | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| low | schema | OpenVibe.Live:woodcut_profile | in production (rs-companion.db) but no CREATE TABLE in the repo; classified frozen-legacy -> OpenVibe.Games (import source) |
| info | env | OpenVibe.Games:NODE_ENV | set in games.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Games:PORT | set in games.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:MIN_CASHOUT | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:RS_COMPANION_DB_PATH | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:RS_PASSTHROUGH_ROBOTS | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Live:UNSAFE_WORDS_PATH | set in live.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:PASTES_MOVED_TO | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:PORT | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:RTP_PORT_MAX | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Media:RTP_PORT_MIN | set in media.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Network:NOTIFICATION_EMAIL_CRITICAL_ONLY | set in network.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Network:NOTIFICATION_MAX_AGE_DAYS | set in network.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Network:NOTIFICATIONS_ENABLED | set in network.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Tools:MEDIA_PUBLIC_URL | set in tools.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | env | OpenVibe.Tools:MEDIA_URL | set in tools.env but not referenced by scanned code (may be read by a dependency or stale) |
| info | schema | OpenVibe.Community:analytics_daily | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Community:analytics_events | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Community:analytics_hourly | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Community:analytics_rate_tracking | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:channel_sounds_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:chat_messages_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:emotes_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:robotstreamer_integrations_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Live:stream_controls_new | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Media:analytics_daily | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Media:analytics_events | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Media:analytics_hourly | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Media:analytics_rate_tracking | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| info | schema | OpenVibe.Network:admin_rate_limits | declared in code but absent from every production database for this service (lazy/unused/legacy migration) |
| unknown | host | powerchat.gg, raspi/hobo.tools (LAN) | unreachable from this environment; state not verified |
| unknown | provider | B2/R2/Cloudflare/PayPal consoles | console configuration (billing, lifecycle rules, zone settings, app config) is not visible from this environment |
