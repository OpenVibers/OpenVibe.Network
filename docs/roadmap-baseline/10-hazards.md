# Hazard register

> Generated by `scripts/roadmap-baseline/generate.js`. Do not edit by hand; edit `data/*.json` or the extractor and regenerate.

Deliverable 9. **Not yet reviewed by the production-host owner** (Wave 0 exit criterion EC7). "Check" is re-evaluated on every run.

| ID | Hazard | Severity | Status | Owner | Wave | Mitigation | Check |
|---|---|---|---|---|---|---|---|
| H1 | Restarting Live drops active RTMP streams | high | open | Live maintainers | W7 (OpenRe extraction removes the class); W21 encodes the rule in deploy tooling | Deploy Live with deploy.sh --wait-idle after checking /api/streams; never restart mid-broadcast for UI-only changes (static deploys need no restart). | - |
| H2 | Stopping the Live socket unit causes 502s | medium | open | Platform operator | W21 | Restart only openvibe-live.service; poll /api/ready (boot ~6s). | openvibe-live.socket is active/running |
| H3 | Media restart during a recording orphans finalisation | high | open | Media maintainers | W4 (reconciliation + duration repair) / W7 (recording ownership to OpenRe) | Do not restart Media while a recording is active; reconcile vods-orphans/ before any retention change. | - |
| H4 | managed_streams.stream_key values still need rotation after a past leak | high | open | Live maintainers (security track S) | W7 (openre.key.rotate) - or earlier as a standalone rotation | Ship per-stream key regeneration and rotate every key; notify affected streamers. | - |
| H5 | Multilingual Whisper model missing breaks non-English captions | medium | mitigated | Live maintainers | - | Install the model and set WHISPER_MODEL_MULTI in live.env. | WHISPER_MODEL_MULTI is set in live.env |
| H6 | YouTube refuses the server IP; the yt tool needs a proxy | low | open | Platform operator | W11 | Set YT_PROXY and/or YT_COOKIES_FILE for openvibe-tools-yt. | YT_PROXY still unset in tools.env |
| H7 | Workstation root disk near full; headless Chrome can hit ENOSPC | low | open | Workstation owner | Track Q | Browser harness must support a remote/containerised runner; prune ~/.claude transcripts (~1.2 GB). | workstation / is 82% used (at generation time) |
| H8 | A hardcoded internal secret was previously public | high | open | Platform operator (security track S) | W1 (scoped service principals) / Track S | Rotate INTERNAL_API_KEY per service and the Cloudflare token; move to scoped service tokens. | - |
| H9 | Removed features and copy rules must not be undone | medium | standing-rule | Every product wave | all | Product waves check the prohibitions list (roadmap 9.1) before adding copy or campaigns. | - |
| H10 | Commit identity | medium | open | Every contributor | all | Per-repo git config user.email = Contact@OpenVibe.Network; review history before push. | main history violates the rule in: Live (238 commits with assistant trailers); Network (14 commits with assistant trailers); Media (30 commits with assistant trailers); Tools (9 commits with assistant trailers) |
| H11 | analytics_events grows without pruning | medium | open | Network maintainers (canonical openvibe-shared/analytics.js) | Track O / ADR-021 - before any schema wave touching Live or Network | Add a retention job that deletes raw rows older than the rollup window, then VACUUM in a maintenance window. | live/data/analytics.db 1.75 GB; network/data/network.db 766.5 MB; tools/apps/audio/data/analytics.db 59.9 MB |
| H12 | No Cloudflare cache-purge step after deploys | low | open | Platform operator | W21 / Track I | Add a purge-scoped token and a purge step, or short max-age + stale-while-revalidate on non-hashed assets. | - |
| H13 | Production drifts from main | medium | open | Platform operator | W0 (checklist) / W21 (release manifests) | Deploy or record why each lagging service is held back. | games: deployed 43c11f0 has the same file tree as origin/main 5710287 but a different SHA (pre-rewrite history); reset the checkout so later pulls fast-forward |
| H14 | Dead and backup database files on the production host | low | open | Live maintainers | W0 | Confirm no code opens them, then delete the empty files and move the backup to /opt/backups with a retention date. | live/data/backups/live-pre-643a2e6.db, live/data/hobo.db, live/data/openvibe.db |

## Constraints

- **H1** (claude-context deploy-live): RTMP ingest is owned by the Live Node process; a restart disconnects every RTMP broadcaster.
- **H2** (claude-context deploy-zero-downtime): Live uses systemd socket activation; stopping openvibe-live.socket drops the listening socket.
- **H3** (claude-context media-vod-offload; roadmap 2.7.6): Recording/finalisation runs inside the Media process. 55 objects / 14.50 GB already sit under B2 vods-orphans/ (21 Sep 2026).
- **H4** (claude-context seadragon-engineering-pass): Leaked keys remain valid until rotated; a rotation tool was declined earlier.
- **H5** (claude-context japaneseoldguy-i18n): Without WHISPER_MODEL_MULTI transcribe.js falls back to the English model. _Evidence: 2026-09-22: WHISPER_MODEL_MULTI is set in /etc/openvibe/live.env and the file it names exists on openvibe-oregon (checked with test -f; path not recorded)._
- **H6** (claude-context tools-registry-domains): Environment problem, not code: yt downloads fail from the host address. _Evidence: 2026-09-22: neither YT_PROXY nor YT_COOKIES_FILE is set in /etc/openvibe/tools.env._
- **H7** (claude-context seadragon-engineering-pass): Browser acceptance runs on the workstation fail when the disk fills.
- **H8** (claude-context): Any surviving openvibe-internal-* value and the Cloudflare token must be treated as exposed.
- **H9** (claude-context rs-promo, network-shared-systems, afresume-site, currency-model): No RobotStreamer promo campaign, no 'free/$0/no ads' copy, never 'Earn Vibes', never call the old theme 'classic'.
- **H10** (claude-context commit-identity): Commits must be OpenVibers <Contact@OpenVibe.Network> with no assistant trailers or co-author lines.
- **H11** (roadmap 2.7.2): Raw analytics_events is never pruned although hourly/daily rollups exist; Live analytics.db and network.db are the estate's largest stores.
- **H12** (roadmap 2.7.5): A cacheable static asset stays stale at some edges until max-age expires; the only Cloudflare token is DNS-01 scoped.
- **H13** (this baseline (prod-snapshot.json)): A service running an older commit than main means repo-based reasoning does not describe production.
- **H14** (this baseline (prod-snapshot.json)): Zero-table openvibe.db/hobo.db and a 260 MB pre-migration backup sit in /opt/openvibe.live/data; nothing records whether they are needed.
